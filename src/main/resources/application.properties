# Server settings
server.port=443
server.error.include-message=always

# SSL configuration
server.ssl.enabled=true
server.ssl.key-store=classpath:keystore.p12
server.ssl.key-store-password=passw
server.ssl.key-store-type=PKCS12
server.ssl.key-alias=allias

# Spring application
spring.application.name=BotZomboid

# Database configuration

# Telegram bot settings
telegram.webhook-path=someWebhookPath
telegram.bot-name=ZomboidBot
telegram.bot-token=someToken

config.lua.path=/home/masa/pzserver_SandboxVars.lua
